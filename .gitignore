# Dependencies (legacy commits track some node_modules; keep new ones out)
node_modules/

# Build output
dist/
.next/
out/
build/

# Python virtualenv
.venv/
__pycache__/

# Editor / tooling / scratch
.cursor/
.vscode/
.playwright-mcp/
graphify-out/

# Built mobile artifacts (the driver APK is ~18MB; build it, don't commit it)
*.apk
*.aab

# Ad-hoc media dropped in the repo root (voice notes, screen recordings)
*.ogg
*.m4a

# Backend runtime data & secrets (keep osrm config, drop the 2.5GB map data)
fuel-backend/osrm/data/
fuel-backend/uploads/
fuel-backend/fcm-service-account.json
fuel-backend/google-services.json
fuel-backend/dist/

# OS
.DS_Store
Thumbs.db
